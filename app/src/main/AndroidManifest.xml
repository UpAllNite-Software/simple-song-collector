<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="com.open.simplesongcollector.SimpleSongCollectorApp"
        android:allowBackup="true"
        android:icon="@mipmap/music_folder"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.SimpleSongFinder">
        <activity
            android:name="com.open.simplesongcollector.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SimpleSongFinder.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>